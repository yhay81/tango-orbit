# Security

Report a vulnerability privately through GitHub private vulnerability reporting.

The service applies a restrictive Content Security Policy, rejects cross-site telemetry, validates the complete telemetry schema, rate-limits writes, hashes random client identifiers, and retains only allowlisted aggregate events.

Do not include private data, credentials, or live exploit details in a public issue.
